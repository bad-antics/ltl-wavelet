// examples/demo.ltl — runnable demo for ltl_wavelet

import std::io
use ltl_wavelet as lib

fn main() {
    io::println("ltl_wavelet demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}